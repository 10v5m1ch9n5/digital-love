const { risibankAPI } = require('./../../axios-instances.js');

module.exports = {
	command: async (message, argv) => {
		message.channel.send('Non implémenté :sleeping:');
		//TODO: implémenter
	}
}
